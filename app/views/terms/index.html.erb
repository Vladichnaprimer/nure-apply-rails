<div class="terms">
  <p id="notice"><%= notice %></p>

  <h1>Редагування термінів</h1>

  <table class="table table-hover">
    <tbody>
    <% @terms.each do |term| %>
        <tr>
          <td><%= term.title %></td>
          <td><%= term.text %></td>
          <% if user_signed_in? && current_user.admin? %>
            <td><%= link_to 'Редагувати', edit_term_path(term) %></td>
            <td><%= link_to 'Видалити', term, method: :delete, data: {confirm: 'Ви впевнені?'} %></td>
          <% end %>
        </tr>
    <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Додати термін', new_term_path, class:"cta cta--inverse cta--inline" %>

</div>
